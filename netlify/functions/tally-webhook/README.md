# tally-webhook (Netlify Functions)

HTTP TypeScript function for Tally form `dWyO7y`. See `docs/tally-webhook-netlify.md`.

Production URL path: `/.netlify/functions/tally-webhook`

```bash
# From repo root
npm install
npx tsx netlify/functions/tally-webhook/scripts/run-fixtures.ts
```
