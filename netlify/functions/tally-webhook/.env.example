# Set these in the Netlify UI (Site settings → Environment variables), not in source.
# TALLY_SIGNING_SECRET: required in production (CONTEXT=production) — fail-closed.
# Unset only for local fixture curls.
SUPABASE_URL=https://odskyepivuzritmbehws.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
TALLY_SIGNING_SECRET=
RESEND_API_KEY=
REPORT_TO_EMAIL=brian@septicaudit.com
