# DEPRECATED: Tally webhook on Google Cloud Functions Gen2

**Superseded.** Hosting moved to **Netlify Functions**.

→ See **[`docs/tally-webhook-netlify.md`](./tally-webhook-netlify.md)** for deploy, Netlify env vars, and Tally hookup.

The `gcp/functions/tally-webhook/` tree is kept only as historical reference. Do not create Secret Manager secrets or deploy Gen2 for this webhook.
